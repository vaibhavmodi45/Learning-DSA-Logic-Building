//This program let us know to implement and initialize the variables and how they can be used to simplify the code.

/* Here we are learning following things like:

#1 How to define and initialize variables.
#2 How to use them in our code.
#3 How to use datatypes to define different kind of data within our program.
#4 How to define single line and multi line comments.


*/

#include<iostream>
using namespace std;

int main() {
    int a = 5;    // This show the variable a which will store the interger value that is defined as 5.
    float pi = 3.14;  //This line of code shows that the variable pi will store the floating point value which is 3.14.
    char myName = 'v';   //This line of code shows that the variable myName store the character value which is v.
    bool eligible = false;   //This line of code shows that the variable eligible stores the boolean value that is false.
    double price = 10.3333;   //This line of code shows that the variable eligible stores the boolean value that is false.
    cout << a << endl;
    
    cout << pi << endl;
    cout << myName << endl;
    cout << eligible << endl;
    return 0;
}


// We can use the 